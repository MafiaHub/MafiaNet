name: Build & Test

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  # Linux: Build and test in Docker container (full network access)
  linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build and test in Docker
        run: |
          docker build -t mafianet-test .
          docker run --rm mafianet-test

  # macOS: Build only (network tests don't work on macOS runners)
  macos:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install OpenSSL
        run: brew install openssl@3

      - name: Configure CMake
        run: |
          cmake -B build \
            -DOPENSSL_ROOT_DIR=$(brew --prefix openssl@3) \
            -DMAFIANET_BUILD_SAMPLES=ON

      - name: Build
        run: cmake --build build --config Release --parallel 4

  # Windows: Build only (network tests don't work on Windows runners)
  windows:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure CMake
        run: cmake -B build -DMAFIANET_BUILD_SAMPLES=ON

      - name: Build
        run: cmake --build build --config Release --parallel 4
